import express from 'express';
const router = express.Router();

const walletController = require('../controllers/walletController');

// router.get('/signIn', walletController.signIn);

module.exports = router;